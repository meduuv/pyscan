[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "pyscan"
version = "0.1.0"
description = "Small dependency-free Python source scanner"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "meduuv"}]

[tool.setuptools.packages.find]
where = ["src"]
